#include <iostream>
using namespace std;

int main () {
    for (int count = 10; count >= 0; count--) {
        if (count == 5) {
            continue;
        }
        cout << count << endl;
        if (count == 0) {
            cout << "Blastoff!" << endl;
        }
    }
    return 0;
}
